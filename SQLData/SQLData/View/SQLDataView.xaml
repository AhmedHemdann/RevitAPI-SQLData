<Window x:Class="SQLData.View.SQLDataView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SQLData.View"
        mc:Ignorable="d"
        Title="Data Wrapper" Height="200" Width="300">
    <Grid>

        <Grid.RowDefinitions>
            <RowDefinition Height="0.2*"/>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <Button Content="Create SQL Table" Grid.Row="1" Margin="10" Background="#D0D4CA" Command="{Binding CreateSQLTableCommand}"></Button>
        <Button Content="Export Category Data" Grid.Row="2" Margin="10" Background="#D0D4CA" Command="{Binding ExportDataCommand}"></Button>
        <Button Content="Import Category Data" Grid.Row="3" Margin="10" Background="#D0D4CA" Command="{Binding ImportDataCommand}"></Button>

    </Grid>
</Window>
